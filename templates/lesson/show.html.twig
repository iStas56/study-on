{% extends 'base.html.twig' %}

{% block title %}{{ lesson.title }} / {{ course.title }} / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.title }}</h1>

    <a href="{{ path('app_course_show', {'id': course.id}) }}"><h3>{{ course.title }}</h3></a>

    <p>{{ lesson.content }}</p>

    <a href="{{ path('app_lesson_index') }}">back to list</a><br>

    <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">edit</a><br>

    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
